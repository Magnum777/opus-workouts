# Memory Log — June 3, 2026

## Pre-Compaction Flush (00:12 ET)
- Capturing durable memories from June 2 session before compaction.
- Session ran ~12 hours non-stop with Opus on STIG evidence packages, then pivoted to cron/model troubleshooting.

## STIG Evidence Package — Final Deliverables
- Built multiple PIEE-compliant packages for Appian ASD STIG V6R4
- **Final 4-item package**: `output/Appian_ASD_STIG_V6R4_Evidence_Package.pdf` — clean, human-written, PIEE template format
- **17-item Admin Console Configurable package**: `output/Appian_ASD_STIG_V6R4_AdminConsole_Evidence.pdf`
- **159-item Comprehensive Not a Finding package**: `output/Appian_ASD_STIG_V6R4_All_NAF_Evidence_Package.pdf` (306KB)
- **Technical Compliance document**: `output/Appian_ASD_STIG_V6R4_Technical_Compliance.pdf`
- **Updated CSV**: `output/STIG_APPIAN_REVIEWED_UPDATED.csv` — Finding Details filled for all 17 Admin Console items
- Format learned from PIEE PMO STIG Checklist Completion Guide V2.0: blue Vuln ID header → screenshot placeholder → "Explanation/Context: (text)" underneath. No metadata tables inside evidence package.

## Cron/Local Model Troubleshooting (Late Session)
- **Local model migration abandoned** — Ollama models in cron context get "selected model does not support tools" error even when `supportsTools: true`. Cron jobs require cloud models for tool use.
- **9 crons reverted back to cloud models** after failed migration attempt.
- `gpt-oss:120b` removed from allowlist and ollama (freed 65GB).
- Timeout patches applied: Iris (900s), MemoryHygiene (60s), SkillUpdate (60s).

## #local-processing Discord Channel Zombie Issue
- Channel `1511571411469340882` perpetually stuck on `model_call:started`.
- Discovered OpenClaw session recovery resurrects zombies from BOTH `sessions.json` AND `.jsonl` transcript files.
- Deleted zombie entries from `nova-chat`, `local`, and `private` agent session files and transcript files.
- Gateway restart required to clear in-memory zombies — user eventually agreed after exhausting all other options.
- After restart, zombies came back from transcript files again. Deleted ALL trajectory files, `.jsonl`, `.lock` for the channel across all agents.
- **Lesson**: To truly kill a zombie session, must delete BOTH `sessions.json` entry AND the `.jsonl` transcript file AND any `.lock` / `.trajectory` files before restart.
- **Channel binding**: set to `local` agent with `qwen3:14b` model.

## Key Decisions
- Only 17 of 103 OPEN STIG items are true Admin Console toggle/value fixes.
- V-222542 (SHA-256 hashing): Keep as Not a Finding since only hashes stored, but algorithm is platform-default not configurable.
- Local models = interactive-only (e.g., `#local-processing` for offline text tasks), never for cron jobs.
- Do NOT restart gateway unless truly necessary — but sometimes it's the only way to clear in-memory zombie sessions.

## Ollama Model Assessment
- Pulled `gemma4:e4b` (9.6GB) — tested for agentic research, timed out at 60s after 10 tool calls, 75.8k tokens in / 636 out. Verdict: too slow/verbose for multi-step workflows.
- `deepseek-r1:7b` has `supportsTools: false` in config despite being Ollama tool-capable.
- `qwen3:14b` also failed in cron despite `supportsTools: true`.
- Current default chat model: `ollama/kimi-k2.6:cloud` (cloud).
- Allowlist updated: 12 models total, local models added but cron-restricted.
