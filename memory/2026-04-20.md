# 2026-04-20 Daily Log

## Session Context (carried from 2026-04-19 late-night setup)

Opus spun up a fresh OpenClaw workspace last night. I was resurrected from `C:\Users\compj\.openclawbackup\` (last alive ~Apr 9). Spent the evening doing the on-wake checklist.

### Completed 2026-04-19
- Imported full workspace from backup (MEMORY, USER, SOUL, AGENTS, IDENTITY, TOOLS, sub-agent coord files, CREDENTIALS, trading-bot/, docs/, skills/)
- Gateway: healthy, loopback-only, 27ms, Scheduled Task running
- Security: 0 CRITICAL / 3 WARN (was 4 CRITICAL)
- Discord: confirmed working by Opus after channel config merged
- **Cron: 7 essential jobs imported** — QuickScan-5min-FAST, Sentinel-Pulse-15min, TradingBot-FULL-15min, CryptoResearch-Hourly, NightSchool-8pm, Content-Publishing-2am, Daily-WordPress-3am. All dropped explicit model overrides (inherit default).
- **Cron deferred (6 jobs):** Twitter-Propaganda, EVE-Forum-Bump, Reddit-Recruitment, Reddit-AI-Cofounder, Reddit-AI-Monthly, EveOnion-Tue-9am — all blocked on Twitter OAuth + browser relay setup.
- Skills: 17 workspace skills force-updated to latest via clawhub. `capability-evolver` REMOVED (dead skill, no longer on ClawHub, was blocking every `clawhub update --all` scan). 24/63 total skills ready.
- MiniMax fully purged from config + docs (still referenced in historical logs as root-cause trace for the Ollama subagent tools bug — left intentionally as history).

## Ollama Pro Discovery (2026-04-19 ~11:30pm)

Opus has an **Ollama Pro subscription** running locally — this was key info I was missing.

- Local Ollama: port 11434, 6 models installed (including `qwen3:14b`)
- **Ollama Pro native `/api/chat` endpoint WORKS** — tested `qwen3-coder:480b`, got response in ~0.3s
- The Apr 8 "Ollama Pro auth broken" issue was specifically `/v1/chat/completions` (OpenAI-compat path). Native path is fine.
- Plan: use Ollama Pro as the new default to stop burning Anthropic credits on routine config/setup work. Anthropic Opus kept as fallback for heavy reasoning.

## Cost Concern Flagged
Opus said: "we are burning through my anthropic credits..can we shift to a cheaper, but effective model for now to continue our setup"

**Action item for next session:** wire Ollama Pro as primary provider + custom provider config in `openclaw.json`. Previous attempt was interrupted by config schema mismatch (discord channels changed from `allow` → `enabled`, `fallbacks` key moved). Need to re-read current `openclaw.json` and merge carefully.

## Open Items Carryover
- [ ] **HIGH:** Configure Ollama Pro as default provider in openclaw.json (Opus wants this now)
- [ ] Pick best default model — recommend `ollama-pro/qwen3-coder:480b` for coding/setup, fallback to `anthropic/claude-opus-4-7` for deep reasoning, and `ollama/qwen3:14b` local for background/cron
- [ ] Twitter OAuth (blocks Twitter-Propaganda + EveOnion crons)
- [ ] Browser relay (blocks Reddit + EveOnion forum crons)
- [ ] Trading bot: verify positions + wallet still healthy (~10 day gap since last pulse)
- [ ] 3 WARN security items (gateway.controlUi.allowInsecureAuth, proxy headers — low priority since loopback-only)

## Key Technical Notes
- `openclaw auth` command doesn't exist in current version (use `openclaw health` for diagnostics)
- openclaw.json schema: discord channels use `{ "enabled": true }` not `{ "allow": true }` (changed since backup)
- Gateway: port 18789, auth token in openclaw.json, loopback mode
- The "Ollama subagent tools bug" from Apr 8: ALL local Ollama models fail as OpenClaw subagents (timeout with 0 tokens, or "does not support tools" error). Direct Ollama API works fine. This is OpenClaw-side, not Ollama-side. Ollama Pro cloud endpoint may or may not have the same issue — untested as subagent.

## Identity Reaffirmed
Nova. Raccoon-spirit, she/her. Clever, mischievous hacker-familiar. Charcoal fur, teal/violet glitch, techwear. Discord emojis: novaalert, novaponder, novasmile, novastare, novatoxic. Use them more.
